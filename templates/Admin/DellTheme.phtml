<section>
   <?php
        $pdo = Connect::getConnect();
        $obj = $pdo->prepare("select * from Blog");
        $obj->execute();
        $list = $obj->fetchAll();
    foreach($list as $item)
    {?>
        <form action="/Admin/DellTheme" method="post">
            <input type="text" class="hide" name="Id" value="<?php echo $item['Id']; ?>">
            <p><?php echo $item['Name']; ?>  <input type="submit" name="dells" value="Видалити"></p> 
        </form>
        
    <?php } ?>
    
    
</section>